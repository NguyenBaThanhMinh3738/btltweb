<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khôi phục mật khẩu thành công</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="forgot-password-container">
        <h1>Khôi phục mật khẩu</h1>

        <!-- Hiển thị thông báo thành công nếu có -->
        <% if (typeof message !== 'undefined') { %>
            <div style="color: green; font-size: 16px; margin-bottom: 15px;">
                <%= message %>
            </div>
        <% } %>

        <p style="font-size: 1.2em;">
            Kiểm tra hộp thư của bạn và làm theo hướng dẫn để khôi phục mật khẩu.
        </p>

        <form action="/forgotPassword" method="post" class="forgot-password-form">
            <button type="submit" class="forgot-password-btn" style="color:rgb(200, 195, 189);">Quay lại</button>
        </form>

        <p style="color: rgb(200, 195, 189); font-size: 0.9em;">
            Đã nhớ mật khẩu? 
            <a href="/login" style="text-decoration: none; color: #007BFF;">Đăng nhập</a>
        </p>
    </div>
</body>
</html>
