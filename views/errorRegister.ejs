<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lỗi Đăng Ký</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="forgot-password-container">
        <h1>Lỗi Đăng Ký</h1>

        <!-- Hiển thị thông báo lỗi nếu có -->
        <% if (typeof message !== 'undefined') { %>
            <div style="color: red; font-size: 16px; margin-bottom: 15px;">
                <%= message %>
            </div>
        <% } %>

        <!-- Form để gửi lại yêu cầu đăng ký -->
        <form action="/register" method="post" class="register-form">
            <button type="submit" class="register-btn" style="color:rgb(200, 195, 189);">Gửi lại yêu cầu đăng ký</button>
        </form>

        <!-- Link quay lại trang đăng nhập -->
        <p><a href="/login">Quay lại trang đăng nhập</a></p>
    </div>
</body>
</html>
